declare class Clipboard {
    private static errorStyleClass;
    static validTrueValues: string[];
    static validFalseValues: string[];
    static tabChar: string;
    static newLineChar: string;
    static getPasteDataType(data: string): Clipboard.PasteDataType;
    private static getCellsAsArrayOfRows();
    private static getSelectedCellDataAs2DArray();
    private static getSelectedRowDataAs2DArray();
    static getCopiedData(canSelectIndividualCells: boolean, rows: GridItem[], columns: GridColumn[]): string;
    static processPastedData(grid: GridContext, data: string): void;
    private static actionIsNotValid(grid, data, leftHandColumn, originRow);
    private static processPastedDataRows(grid, data);
    private static processPastedDataRow(grid, dataRow, row, startColumn?);
    private static processPastedCell(grid, data, row, column, showErrorMessageIfFail?);
    private static getValue(data, column);
    private static publishError(message);
    static tooManyColumnsMessage(): void;
    static columnIsNotEditableMessage(path: string): void;
    static noColumnIsEditableMessage(): void;
    static unknownDataFormatMessage(): void;
    static pasteStringErrorMessage(): void;
    static tooManyRows(): void;
}
declare module Clipboard {
    enum PasteDataType {
        Unknown = -1,
        String = 0,
        Rows = 1,
    }
}
export = Clipboard;
